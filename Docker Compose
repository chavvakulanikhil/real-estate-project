services:

  realestate:

    image: real-estate-app

    container_name: real-estate-container

    ports:
      - "80:80"

    restart: always

  nginx:

    image: nginx:latest

    container_name: nginx-container

    ports:
      - "8081:80"

    restart: always
